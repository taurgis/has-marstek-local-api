---
"ha-marstek-release-tools": patch
---

Log routine setup, discovery and service activity at debug level, so the default log keeps only the connection-loss, recovery and device-change events worth reading.
