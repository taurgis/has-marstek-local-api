---
"ha-marstek-release-tools": patch
---

Log one warning when a device stops answering and one line when it comes back, instead of a warning for every failed request of every failed poll, and stop republishing the same repair issue each cycle.
