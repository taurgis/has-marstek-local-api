---
"ha-marstek-release-tools": patch
---

Raise validation errors instead of generic errors when a mode or depth-of-discharge value is rejected, so Home Assistant shows the reason without a stack trace.
