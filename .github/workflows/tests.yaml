name: Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  tests:
    name: Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Latest pytest-homeassistant-custom-component / Home Assistant Core
        # 2026.9 require Python >=3.14.2.
        python-version: ["3.14"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_test.txt

      - name: Lint
        run: python -m ruff check custom_components/marstek/

      - name: Type check
        run: python -m mypy --strict custom_components/marstek/

      - name: Run tests
        run: pytest tests/ -v --tb=short --cov=custom_components/marstek --cov-report=term-missing --cov-fail-under=95

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.14'
        with:
          fail_ci_if_error: false
